[package]
name         = "qv-consensus"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
authors.workspace      = true
description  = "Ouroboros Praos PoS engine (slot leader, VRF, KES, finality)."

[lints]
workspace = true

[dependencies]
qv-common  = { workspace = true }
thiserror  = { workspace = true }
tracing    = { workspace = true }
serde      = { workspace = true }
qv-core    = { path = "../qv-core" }
qv-crypto  = { path = "../qv-crypto" }
# qv-storage kaldırıldı: consensus katmanı storage'a doğrudan bağımlı değil;
# chain_state in-memory, kalıcılık qv-storage tarafından compose edilecek.

[dev-dependencies]
proptest = { workspace = true }
